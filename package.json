{
  "name": "instagram-analyzer",
  "version": "1.0.0",
  "description": "Analisador de perfis do Instagram com IA",
  "scripts": {
    "install-all": "cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
